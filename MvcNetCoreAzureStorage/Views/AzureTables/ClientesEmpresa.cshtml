@model IEnumerable<MvcNetCoreAzureStorage.Models.Cliente>

@{
	ViewData["Title"] = "ClientesEmpresa";
}

<h1>Clientes Empresa</h1>

<form method="post">
	<label>Introduzca nombre de empresa</label>
	<input type="text" name="empresa" class="form-control" />
	<button class="btn btn-outline-dark">Buscar clientes</button>
</form>

@if (Model != null)
{
	<table class="table table-warning">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(model => model.Nombre)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Edad)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Salario)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Empresa)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.IdCliente)
				</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.Nombre)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Edad)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Salario)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Empresa)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.IdCliente)
					</td>
				</tr>
			}
		</tbody>
	</table>
}
